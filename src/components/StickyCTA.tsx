export function StickyCTA() {
  return (
    <a
      href="#book"
      className="fixed bottom-5 left-1/2 z-40 -translate-x-1/2 rounded-full bg-primary px-7 py-3.5 text-sm font-bold text-primary-foreground shadow-glow transition hover:scale-105 md:hidden"
    >
      Book Now
    </a>
  );
}
